<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.team3.prj.dao.RoomDtlDAO">
	<select id="roomDtlSelectAll" resultType="com.team3.prj.vo.RoomDtlVO">
		SELECT id, mst_id, room_name, num_of_guests, check_in_time,
				check_out_time, weekday_price, weekend_price,
				pic1_org, pic1_uuid, pic2_org, pic2_uuid, pic3_org, pic3_uuid
		FROM room_dtl
	</select>

<!-- 	<insert id="qnaInsert"> </insert> resultType="java.lang.String">
		INSERT INTO 
		(
               id,
		       nickname, no, cate_id,
		       is_pub, title, q, c_user
        )
		VALUES 
        (
               seq_qna.nextval, 시퀀스값 받아옴 seq_qna
		       #{nickname}, #{no}, #{cateId},
		       #{isPub}, #{title}, #{q}, #{cUser}
        )
    
        <selectKey keyProperty="id" resultType="Long" order="AFTER"> 
          SELECT seq_qna.CURRVAL FROM DUAL 
        </selectKey> 
	</insert> -->

</mapper>
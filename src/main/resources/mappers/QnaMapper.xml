<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.team3.prj.dao.QnaDAO">
	<!-- 전체 출력 sql문 -->
	<select id="selecaAll" resultType="com.team3.prj.vo.QnaVO">
		SELECT nickname, no,
		cate_id, is_pub, title, q, a
		FROM qna
	</select>

	<!-- 1개 출력 sql문 -->
	<select id = "selectOne" resultType="com.team3.prj.vo.QnaVO">
		SELECT *
		FROM qna
		WHERE id = #{id}
		ORDER BY id
	</select>
	
	<!-- 검색기능 sql문 -->
	<select id="search" resultType="com.team3.prj.vo.QnaVO">
		SELECT *
		FROM qna
		<where>
		<if test='cateIdValue!= null and !"".equals(cateIdValue)'>
		AND	cate_id = #{cateIdValue}
		</if>
		<if test='keyWord!= null and !"".equals(keyWord)'>
			<if test='searchType == "title"'>
				AND title LIKE '%'||#{keyWord}||'%'
				</if>
			<if test='searchType == "nickname"'>
				AND nickname LIKE '%'||#{keyWord}||'%'
				</if>
			</if>
		</where>
		ORDER BY id
	</select>

	<!-- 게시글 등록 sql문 -->
	<insert id="insert">
		<selectKey keyProperty="id" resultType="Long" order="BEFORE">
		 <!-- order="BEFORE" - 아래 insert문 실행전 실행 -->
			SELECT seq_qna.NEXTVAL FROM DUAL
		</selectKey>

		INSERT INTO qna
		(
		id,
		nickname, no, cate_id,
		is_pub, title, q, c_user
		)
		VALUES
		(
		#{id}, <!-- 시퀀스 ID값이 들어감. -->
		#{nickname},
		(SELECT NVL(MAX(no),0) + 1 FROM qna), <!-- #{no}, -->
		#{cateId},
		#{isPub}, #{title}, #{q}, #{cUser}
		)
	</insert>

	<!-- 등록된 게시글 수정 sql문 -->
	<update id="updateQ">
		UPDATE qna
		SET title=#{title}, q=#{q},
		cate_id=#{cateId}, is_pub=#{isPub}
		WHERE id=#{id}
	</update>

	<!-- 답변달기 sql문 -->
	<update id="updateA">
		UPDATE qna
		SET a=#{a}
		WHERE id=#{id}
	</update>
	
	
	<!-- 게시글 삭제 sql문 -->
	<delete id="delete">
		DELETE qna
		where id=#{id}
	</delete>
	
</mapper>